package br.com.blockfilmes.blockfilmes;

import javafx.application.Application;

public class Main {
    public static void main(String[] args) {
        // Essa classe serve apenas como um "gatilho" puro para enganar o plugin do Maven
        Application.launch(BlockFilmes.class, args);
    }
}